# 🚀 สัปดาห์ที่ 16: การทดสอบคุณภาพและนำเว็บขึ้นระบบจริง (QA & Web Hosting)
**วิชาการเขียนโปรแกรมเว็บ ม.5 | โฟลเดอร์ปฏิบัติการของนักเรียน (Student)**

---

## 🎯 ภารกิจปฏิบัติการประจำสัปดาห์ (3 Missions)

ในสัปดาห์นี้ นักเรียนจะได้สวมบทบาทเป็น **Web Developer & QA Tester** นำแพลตฟอร์มการเรียนรู้ครบทั้ง 4 หน้าขึ้นสู่ระบบ Cloud บนโลกออนไลน์จริง!

---

### 🧩 ภารกิจที่ 1: เชื่อมโยง 4 หน้าเว็บผ่าน Shared Components (Core Mission)
ในโฟลเดอร์นี้ ลิงก์นำทางบางส่วนถูกปลดออก (เป็นเครื่องหมาย `#`) ให้นักเรียนเปิดโค้ดและแก้ไขจุด **TODO** ดังต่อไปนี้:

1. **เปิดไฟล์ `components.js`:**
   * ค้นหาคอมเมนต์ `<!-- TODO 1 -->` ถึง `<!-- TODO 4 -->` ในส่วน Navbar:
     * `TODO 1:` แก้ `href="#"` ของเมนู Index ให้เป็น `href="index.html"`
     * `TODO 2:` แก้ `href="#"` ของเมนู Course ให้เป็น `href="course.html"`
     * `TODO 3:` แก้ `href="#"` ของเมนู Instructors ให้เป็น `href="instructors.html"`
     * `TODO 4:` แก้ `href="#"` ของเมนู Contact Us ให้เป็น `href="contact.html"`
   > 💡 **ข้อสังเกต:** เมื่อนักเรียนแก้ที่ไฟล์ `components.js` เพียงที่เดียว เมนูนำทางใน **ทุกหน้าจะเชื่อมถึงกันทั้งหมดทันที!**

2. **เปิดไฟล์ `index.html`:**
   * ค้นหา `TODO 5:` ที่ปุ่ม **Buy now** ให้เชื่อมไปยัง `href="course.html"`
   * ค้นหา `TODO 6:` ที่ปุ่ม **more** ให้เชื่อมไปยัง `href="course.html"`

---

### 🔍 ภารกิจที่ 2: ด่านตรวจสอบคุณภาพ (QA & Device Testing)
1. **Broken Links Check:** เปิดไฟล์ `index.html` บนเบราว์เซอร์ แล้วทดสอบคลิกเมนูทุกปุ่ม ว่าสามารถกดสลับไปหน้าอื่นและกดกลับมาหน้าเดิมได้ 100% หรือไม่
2. **Mobile Device Test:** กดปุ่ม `F12` เพื่อเปิด Developer Tools แล้วคลิกรูปไอคอน **Device Toggle (มือถือ/แท็บเล็ต)** มุมซ้ายบน:
   * ตรวจสอบว่ารูปภาพไม่ล้นจอ
   * ตรวจสอบว่าเมนูด้านบนยุบเป็นปุ่ม Hamburger และกดเปิด-ปิดได้สมบูรณ์

---

### 🌐 ภารกิจที่ 3: ปล่อยเว็บขึ้นระบบจริง (Deploy to GitHub Pages)
1. เข้าสู่ระบบ [github.com](https://github.com) แล้วสร้าง **New Repository** (ตั้งชื่อ เช่น `learning-platform` และเลือกเป็น **Public**)
2. คลิก **uploading an existing file** แล้วลากไฟล์งานทั้งหมดในโฟลเดอร์นี้ขึ้น GitHub
3. กดปุ่มสีเขียว **Commit changes**
4. ไปที่เมนู **Settings** ➡️ **Pages** (แถบซ้ายมือ)
5. ตรงหัวข้อ **Branch** ให้เปลี่ยนจาก `None` เป็น `main` แล้วกด **Save**
6. รอประมาณ 1-3 นาที จะปรากฏลิงก์จริง เช่น:
   ```text
   https://<username>.github.io/learning-platform/
   ```
7. นำ URL ไปส่งงานในระบบ หรือส่งให้เพื่อนข้างๆ เปิดทดสอบบนสมาร์ตโฟนจริง!

---

### ⭐ ความท้าทายพิเศษ (Extra Challenges)
* **Custom Theme:** เปิดไฟล์ `style.css` แล้วลองเปลี่ยนค่า `--brand-blue: #0066ff;` เป็นสีประจำตัว เช่น สีเขียวมรกต `#10b981` หรือสีม่วง `#7c3aed` แล้ว Commit ขึ้น GitHub อีกครั้ง
* **QR Code:** นำลิงก์เว็บจริงไปสร้าง QR Code เพื่อให้เพื่อนใช้กล้องมือถือสแกนเข้าชมได้ทันที!
