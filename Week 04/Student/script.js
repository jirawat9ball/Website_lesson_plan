// === คำแนะนำในการเขียน JavaScript ===
// 1. ดึง Element ต่างๆ จาก HTML (เช่น Input น้ำหนัก, ส่วนสูง, ปุ่มคำนวณ, และ Div แสดงผล)
// const weightInput = ...
// const heightInput = ...
// const calcBtn = ...
// const resultDiv = ...

// 2. สร้างฟังก์ชันสำหรับการคำนวณ BMI
function calculateBMI() {
    // ดึงค่า value จาก input และแปลงเป็นตัวเลข (parseFloat)
    
    // ตรวจสอบความถูกต้องของข้อมูล (ไม่ว่าง หรือไม่เป็นค่าติดลบ)
    
    // แปลงส่วนสูงจาก ซม. เป็น เมตร
    
    // คำนวณ BMI = น้ำหนัก / (ส่วนสูงเมตร * ส่วนสูงเมตร)
    
    // แสดงผลลัพธ์ผ่าน innerText
    
    // (Extra) นำผลลัพธ์ไปเขียนเงื่อนไข If-Else เพื่อบอกสถานะสุขภาพ
}

// 3. เชื่อมโยงฟังก์ชันกับปุ่มคำนวณเมื่อมีเหตุการณ์คลิก (addEventListener)
// calcBtn.addEventListener('click', calculateBMI);
