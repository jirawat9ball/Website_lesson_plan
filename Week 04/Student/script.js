// === คำแนะนำในการเขียน JavaScript ===

// 1. ดึง Element ต่างๆ จาก HTML (เช่น ช่องอินพุตซื้อหวย, ปุ่มกดตรวจรางวัล, พื้นที่แสดงเลขรางวัล, และพื้นที่แสดงผลลัพธ์)
// const lottoInput = ...
// const playBtn = ...
// const winningNumDiv = ...
// const resultDiv = ...

// 2. สร้างฟังก์ชันสำหรับเสี่ยงโชค/ตรวจรางวัล
function playLottery() {
    // 2.1 ดึงค่าตัวเลขจากช่องอินพุตและแปลงเป็นตัวเลขจำนวนเต็ม (parseInt)
    
    // (Extra Challenge 1) ตรวจสอบความถูกต้องของข้อมูล (Validation)
    // เช็คกรณีข้อมูลเป็นค่าว่าง (ลืมกรอก) หรือมีค่าน้อยกว่า 0 หรือมากกว่า 99
    // หากพบข้อผิดพลาด ให้แสดงข้อความแจ้งเตือนสีส้ม และหยุดฟังก์ชันทันที (return)
    
    // 2.2 สุ่มเลขท้าย 2 ตัว (0 - 99) โดยใช้ Math.random() และ Math.floor()
    
    // 2.3 แปลงเลขรางวัลสุ่มได้ให้เป็นข้อความ 2 หลัก (เช่น 5 -> "05") เพื่อแสดงผลที่หน้าจอ
    // คำแนะนำ: ใช้คำสั่ง winningNumber.toString().padStart(2, '0')
    
    // 2.4 ตรวจสอบเงื่อนไขการถูกรางวัล (If-Else)
    // เปรียบเทียบเลขที่นักเรียนซื้อกับเลขรางวัลที่สุ่มได้
    // แสดงข้อความและเปลี่ยนสีข้อความตามผลลัพธ์ (เช่น สีเขียวเมื่อถูกรางวัล, สีแดงเมื่อไม่ถูกรางวัล)
    
    // (Extra Challenge 2) ตรวจรางวัล "เฉียด" (+/- 1 จากเลขรางวัล)
    // แสดงข้อความปลอบใจเป็นตัวหนังสือสีส้ม
}

// 3. เชื่อมโยงฟังก์ชันกับปุ่มตรวจรางวัลเมื่อมีเหตุการณ์คลิก (addEventListener)
// playBtn.addEventListener('click', ...);
